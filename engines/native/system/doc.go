// Package system implements cross-platform abstractions for user-management
// access-control and sub-process execution.
package system

import "github.com/taskcluster/taskcluster-worker/runtime"

var debug = runtime.Debug("system")
