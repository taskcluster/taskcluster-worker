// Package worker is responsible for managing the claiming, executing, and
// resolution of tasks.
package worker

import "github.com/taskcluster/taskcluster-worker/runtime/util"

var debug = util.Debug("worker")
